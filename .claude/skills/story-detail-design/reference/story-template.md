# Story 详设文档结构模板

> 本文件为 story-detail-design skill 的文档结构模板。SKILL.md 中通过引用链接加载本文件。

## 设计要素承载总览

Story 详设阶段承载代码模型（C3/C4）、通信模型（M1/M2/M4/M5）、运行模型（R3/R5/R6）、数据处理模型（D1/D2/D3/D4/D5/D6）共 15 项设计要素。核心要素为 C3 接口与实现映射 + M2 消息定义 + M4 通信图 + D3 数据结构(结构体) + D4 处理逻辑，缺一不可。标注 `> **承载设计要素**` 的章节必须填写实质内容。

## 文档模板

```markdown
# Story-X：{Story名称} - 软件实现详设

## 一、需求概述
- Story描述（来源：主设计文档 Story 章节）
- 验收标准（来源：主设计文档 Story 章节）
- 关键机制要点（来源：主设计文档 Story 章节）

## 二、规格文档引用
- 接口规范文档引用（含章节位置）
- 数据模型文档引用（含章节位置）
- 架构设计文档引用（含章节位置）

## 三、代码仓交互流程
> **承载设计要素**：M1 对象角色, M4 通信图/时序图, D1 数据来源, D2 数据流图
- mermaid flowchart（含文件路径和行号）

## 四、复用现有代码分析
- 现有代码 | 文件路径 | 复用方式（来源：模块设计文档 + 代码文件验证）

## 五、新增文件接口契约（不含完整代码实现）
> **承载设计要素**：C3 接口与实现映射, C4 命名规则, M2 消息定义, R3 接口约束, D3 数据结构(结构体)
- 5.1 表结构字段定义（仅字段说明，不含完整SQL）
- 5.2 实体字段定义（仅struct字段，不含方法）
- 5.3 DAO接口签名（仅方法签名，不含实现体）
- 5.4 Service接口签名（仅方法签名，不含实现体）
- 5.5 启动集成点（仅说明集成位置，不含完整代码）

## 六、关键逻辑说明（表格描述，不含完整代码）
> **承载设计要素**：D4 处理逻辑, D5 处理粒度
- 算法流程表格（步骤、决策点、边界条件）
- 核心逻辑片段示例（不超过10行）

## 七、锁机制说明（如有并发，来源：主设计文档 DB 章节）
> **承载设计要素**：R5 容错机制, R6 数据一致性, D6 并发访问(锁机制)

## 八、配置项（来源：主设计文档配置章节）

## 九、测试设计要点（表格描述，不含完整测试代码）
- 9.1 Mock方案表格
- 9.2 UT测试场景表格
- 9.3 DT测试场景表格
- 9.4 测试覆盖率要求

## 十、开发任务清单（来源：主设计文档 Story 章节）
> **承载设计要素**：C3 接口与实现映射（接口文件→实现文件映射）

## 十一、依赖说明
> **承载设计要素**：M5 链(连接器)
```

## 必须包含的内容

| 章节 | 内容 | 说明 | 承载设计要素 |
| --- | --- | --- | --- |
| **需求概述** | 目标、验收标准、关键机制 | 明确要做什么（引用主设计文档） | — |
| **规格文档引用** | 接口契约、数据模型来源 | 精准标注来源文档和行号 | — |
| **交互流程** | mermaid 流程图 | 展示核心流程（含文件路径和行号） | M1 对象角色, M4 通信图/时序图, D1 数据来源, D2 数据流图 |
| **复用现有代码** | 表格列出可复用模块 | 避免重复开发 | — |
| **新增文件接口契约** | 结构体字段、方法签名 | 接口契约（不含实现体） | C3 接口与实现映射, C4 命名规则, M2 消息定义, R3 接口约束, D3 数据结构(结构体) |
| **关键逻辑说明** | 表格描述关键逻辑 | 算法流程、决策点、边界条件 | D4 处理逻辑, D5 处理粒度 |
| **配置项** | 表格列出配置来源、默认值 | 配置说明 | — |
| **锁机制** | 并发场景、锁方案表格 | 并发安全说明 | R5 容错机制, R6 数据一致性, D6 并发访问(锁机制) |
| **测试要点** | 测试类型、场景、验证点表格 | 测试指导（不含完整测试代码） | — |
| **开发任务清单** | 表格列出任务、文件、改动类型 | 任务拆分 | C3 接口与实现映射 |
| **依赖说明** | 前置/后续依赖 | 依赖关系 | M5 链(连接器) |

## 设计要素填写要求（P0/P1/P2 强化）

> 以下填写要求为硬性约束：标注"必须"的条目缺失或不达标时，视为该设计要素未完成，文档需返工。

### P0 填写要求（必须满足）

| 设计要素 | 所在章节 | 必须填写的内容 | 禁止的模糊写法 |
| --- | --- | --- | --- |
| **R5 容错机制** | §七 锁机制说明 | 必须写**回滚调用点**（如 `SNMP失败→s.decrementCount(level)`、`Upsert失败→s.dao.Rollback(ctx)`），明确失败后调用哪个方法、操作哪个对象 | ❌ 仅写"需要回滚""失败后回滚"等无调用点的描述 |
| **TM 测试场景** | §九 测试设计要点 | 必须写**ID范围约束**（附告警ID清单/边界值清单，如"告警ID范围：1001-1099，边界值：1001/1050/1099/1100(越界)"），不只罗列测试用例编号 | ❌ 仅写"测试用例1/2/3"而无ID范围与边界值 |

### P1 填写要求（应当满足）

| 设计要素 | 所在章节 | 必须填写的内容 | 禁止的模糊写法 |
| --- | --- | --- | --- |
| **R1 配置项** | §八 配置项 | 必须写**配置键名+默认值**（如 `snmp::v3_username=empty`、`gids.master.check-period=5s`），标注配置来源章节 | ❌ 仅写"需要配置SNMP用户名""需要配置选主周期" |
| **L1/L3 实体表** | §五 新增文件接口契约 | 必须标注**唯一来源/冗余来源**（如"告警ID：唯一来源=FM回调body.alarmId"、"MOI冗余来源=FM查询+SNMP trap，以FM为准"） | ❌ 不标来源或来源不明确 |

### P2 填写要求（建议满足）

| 设计要素 | 所在章节 | 必须填写的内容 | 禁止的模糊写法 |
| --- | --- | --- | --- |
| **D4 处理逻辑** | §六 关键逻辑说明 | 每个处理步骤**附失败回滚调用点**（如"步骤3: Upsert抢主→失败回滚 `s.dao.ClearMasterFlag()`"），与§七 R5容错保持一致 | ❌ 仅写步骤名/决策点而无失败处理路径 |

### 填写要求检查清单

生成文档后，逐项自检：

| 优先级 | 检查项 | 通过标准 |
| --- | --- | --- |
| **P0** | R5 回滚调用点 | 每个容错场景都有 `失败→具体方法调用` |
| **P0** | TM ID范围约束 | 每个测试场景都有ID清单/边界值清单 |
| **P1** | R1 配置键名+默认值 | 每个配置项都有 `键名=默认值` 格式 |
| **P1** | L1/L3 来源标注 | 每个实体字段都标注唯一来源或冗余来源 |
| **P2** | D4 失败回滚调用点 | 每个处理步骤都有失败处理路径 |

---

## 不应包含的内容（关键约束）

| 内容 | 原因 | 示例 |
| --- | --- | --- |
| **完整方法实现代码** | 代码应在代码文件中，文档只定义接口契约 | ❌ 不应包含超过10行的完整方法实现 |
| **完整测试用例代码** | 测试代码应在测试文件中，文档只列测试要点 | ❌ 不应包含完整的 `func TestXxx()` 实现 |
| **import语句等实现细节** | 属于代码实现，不属于设计文档 | ❌ 不应列出完整的 import 列表 |
| **重复的结构体定义** | 结构体定义一次即可，不要在多处重复 | ❌ 不要在多个章节重复定义同一结构体 |
| **完整的SQL语句** | SQL应在代码文件中，文档仅说明字段 | ❌ 不应包含完整的 CREATE TABLE（仅列出字段） |

## 代码示例规范（严格限制）

**关键代码示例**应该：
- **简洁**：仅展示核心逻辑片段，不超过10行
- **示例**：展示关键调用方式，而非完整实现
- **契约**：展示接口签名，而非实现体

**正确示例**（展示核心调用）：

```go
// 仅展示关键调用方式（不超过10行）
func onBecomeMaster() {
    fmService := NewFmSubscribeService()
    fmService.Subscribe()  // 核心调用：订阅FM告警
}
```

**错误示例**（完整实现体，不应在文档中）：

```go
// ❌ 错误：完整实现体（超过10行，不应在文档中）
func (s *fmSubscribeServiceImpl) Subscribe() error {
    subscribeReq := req.NewFmSubscribeRequest(...)
    bodyBytes, err := json.Marshal(subscribeReq)
    if err != nil {
        return err
    }
    resp, err := s.httpClient.Post("/fmAlarmOpenApi/subscribe/v1", bodyBytes)
    // ... 30行实现代码 ...
}
```

## 文档篇幅建议

| 文档类型 | 建议篇幅 | 说明 |
| --- | --- | --- |
| Story 详设文档 | 100-200行 | 仅接口契约和关键逻辑说明，不含完整代码 |
| 主设计文档 | 300-500行 | 包含完整需求、架构、接口、DB设计 |

> **原则**：文档聚焦"做什么、怎么做"，代码文件聚焦"具体实现"
