---
name: spec-logic-audit
description: 基于多彩建模方法论审核 Spec/需求文档中业务逻辑描述的完备性。当用户需要解读一篇 Spec/需求/设计文档、检查其中业务逻辑是否存在断裂点或描述不全、需要针对逻辑缺口 ask-human 补齐、并将建模结果以 HTML 可视化呈现供编码人员审核时使用。触发场景包括："审核 spec 业务逻辑完备性"、"多彩建模"、"检查需求逻辑断点"、"建模结果生成 HTML 并找人确认"等。
---

# Spec 业务逻辑完备性审核（多彩建模）

核心思想：把 Spec 文档翻译成多彩建模图（业务事件-角色-实体-描述的因果链路图），逻辑断裂点会在图上自然暴露；再用 ask-human 补齐断点，最终产出一份可供编码人员审核的 HTML。

## 工作流程（四阶段，严格按序执行）

### 阶段 1：解读文档，完成多彩建模

1. 通读用户提供的文档全文（Spec/需求/设计文档）。
2. 按 [references/color-modeling.md](references/color-modeling.md) 中的方法论提取四类元素：
   - 粉色「时刻/事件」：业务动作、状态变更、流程节点
   - 黄色「角色」：参与者（人、系统、外部服务）
   - 绿色「实体」：业务对象（人/地/物）
   - 蓝色「描述」：规则、约束、分类、属性说明
3. 按时序/因果箭头连接元素，形成完整建模链路。建模时不臆造文档中不存在的业务规则——不确定的内容标记为「待确认」，留到阶段 2 处理。

### 阶段 2：生成初版 HTML，检查逻辑断裂点，ask-human

1. 复制 [assets/model-template.html](assets/model-template.html) 为初版 HTML，将建模结果填入。
2. 对照 [references/gap-checklist.md](references/gap-checklist.md) 逐项检查逻辑断裂点与逻辑不全，在 HTML 中用红色虚线「断点卡片」高亮每一处。
3. **每发现一处断点，立即用交互提问工具 ask-human**，一次提问聚焦一个断点；提供 2-4 个具体候选答案选项（不允许"其他"类兜底选项由自己提供），并推荐最可能的一项。
4. 将用户的答复记录为断点的「人工裁定」，标注来源；同时在**被审核的 Spec 原文档末尾**的「人工澄清」章节表格中追加一行（格式见下方「人工澄清章节规范」），逐断点逐行追加，全部断点处理完毕后章节即完整。

### 阶段 3：完善 HTML，清晰呈现建模结果

1. 把人工裁定回填到模型中，断点卡片转为「已补齐」状态（绿色边框 + 裁定内容）。
2. 完善 HTML 的可读性：
   - 四色图例置顶，非技术人员也能看懂
   - 因果链路按主流程→分支/异常流程分层排布
   - 每个元素卡片标注：所属颜色类别、原文出处（章节/段落）、确认状态（原文明确/人工补齐/仍存疑）
3. 仍存疑的项保留醒目标记，不得悄悄删除。

### 阶段 4：提醒编码人员审核

在最终回复中明确输出「编码人员审核提醒」，包含：
- HTML 文件位置与查看方式
- 审核要点清单：已补齐的断点裁定是否符合真实业务、仍存疑项、建模中缺失但编码时必须的细节（异常处理、并发、边界值）
- 明确提示：**HTML 未经编码人员确认前，不得进入编码阶段**

## 人工澄清章节规范

ask-human 每得到一条答复，立即在被审核的 Spec 原文档**末尾**追加（若章节已存在则更新）「人工澄清」章节，以表格记录全部人工裁定：

```markdown
## 人工澄清

> 本章节由 spec-logic-audit 审核过程生成，记录审核中发现的逻辑断点及人工裁定结果，供编码人员参考。

| 断点位置 | 断点类型 | 问题描述 | 候选选项 | 人工裁定 | 答复时间 | 严重程度 | 确认状态 |
|---|---|---|---|---|---|---|---|
| 3.2 节 支付流程 | 异常分支缺失 | 支付超时后订单如何处理文档未定义 | ①自动关单（推荐） ②保留待支付 ③转人工介入 | 超时自动关单并释放库存 | 2026-07-28 14:30 | 🔴 阻塞级 | 已补齐 |
```

规则：
- 章节标题固定为 `## 人工澄清`，放在文档末尾；重复审核时更新既有表格，不重复追加章节。
- 每个断点一行，按 ask-human 处理顺序追加。
- 用户跳过/无法确认的断点也必须记录，确认状态填「仍存疑」，人工裁定列填「未确认」。
- 「断点类型」与「严重程度」取值与 [references/gap-checklist.md](references/gap-checklist.md) 中的分类分级保持一致。
- 只追加/更新本章节，**不得改动 Spec 原有内容**。

## 输出规范

- 产出物两份：建模结果 HTML + 被审核 Spec 原文档末尾追加的「人工澄清」章节。
- HTML 为单文件、无外部依赖，可直接在浏览器打开。
- HTML 生成后按网页交付规范保存版本，向用户提供可预览的版本；不要只给一个裸文件路径。
- 配色遵循多彩建模标准色，模板已内置，不要改动色板语义。
- 全程用中文输出建模内容与提问。

## 参考文件索引

- [references/color-modeling.md](references/color-modeling.md) — 多彩建模方法论：四类元素的判定规则、连接规则、建模步骤
- [references/gap-checklist.md](references/gap-checklist.md) — 逻辑断裂点检查清单：9 类断点模式与 ask-human 提问话术
- [assets/model-template.html](assets/model-template.html) — 建模结果 HTML 模板（四色卡片 + 断点高亮 + 人工裁定区）
